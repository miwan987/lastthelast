{
  "buildCommand": "npm install --legacy-peer-deps && npm install vite@5.4.19 @vitejs/plugin-react@4.3.4 --save-dev --legacy-peer-deps && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "BASE_PATH": "/"
  }
}